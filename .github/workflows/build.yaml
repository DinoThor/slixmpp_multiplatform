name: Build and publish

on:
  push:
    branches:
      - '*'
    tags:
      - 'v*'

jobs:
  retrieve-package:
    name: Clone Slixmpp and rename
    runs-on: ubuntu-latest
    steps:
      - name: Download and extract slixmpp source
        run: |
          curl -sSL https://codeberg.org/poezio/slixmpp/archive/slix-1.12.0.tar.gz -o slixmpp.tar.gz
          tar -xzf slixmpp.tar.gz
          cd slixmpp

      - name: Patch project name in pyproject.toml and Cargo.toml
        working-directory: slixmpp
        run: |
          sed -i.bak 's/^name = "slixmpp"/name = "slixmpp_multiplatform"/' pyproject.toml
          sed -i.bak 's/name = "slixmpp"/name = "slixmpp_multiplatform"/' Cargo.toml

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: slixmpp-multiplatform
          path: slixmpp

  build-win:
    name: Build for Win
    needs: retrieve-package
    strategy:
      matrix:
        os: [windows-latest]
        python-version: ['3.9', '3.10', '3.11', '3.12', '3.13', '3.14']

    runs-on: ${{ matrix.os }}
    steps:
      - name: Download slixmpp
        uses: actions/download-artifact@v4
        with:
          name: slixmpp-multiplatform
          path: slixmpp-multiplatform

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install maturin
        run: pip install maturin

      - name: Build wheels with maturin
        working-directory:  slixmpp-multiplatform
        run: |
          maturin build -i python${{ matrix.matrix.python-version }} --release

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.os }}-py${{ matrix.python-version }}
          path: slixmpp-multiplatform/target/wheels/*.whl

  build-macos-silicon:
    name: Build for macOS (Apple Silicon)
    needs: retrieve-package
    strategy:
      matrix:
        os: [macos-latest]
        python-version: [ '3.9', '3.10', '3.11', '3.12', '3.13', '3.14' ]

    runs-on: ${{ matrix.os }}
    steps:
      - name: Download slixmpp
        uses: actions/download-artifact@v4
        with:
          name: slixmpp-multiplatform
          path: slixmpp-multiplatform

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install maturin
        run: pip install maturin

      - name: Build wheels with maturin
        working-directory: slixmpp-multiplatform
        run: |
          maturin build -i python${{ matrix.matrix.python-version }} --release

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.os }}-py${{ matrix.python-version }}
          path: slixmpp-multiplatform/target/wheels/*.whl

  build-macos-x86:
    name: Build for macOS (x86)
    needs: retrieve-package
    strategy:
      matrix:
        os: [macos-13]
        python-version: [ '3.9', '3.10', '3.11', '3.12', '3.13', '3.14' ]

    runs-on: ${{ matrix.os }}
    steps:
      - name: Download slixmpp
        uses: actions/download-artifact@v4
        with:
          name: slixmpp-multiplatform
          path: slixmpp-multiplatform

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install maturin
        run: pip install maturin

      - name: Build wheels with maturin
        working-directory: slixmpp-multiplatform
        run: |
          maturin build -i python${{ matrix.matrix.python-version }} --release 

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.os }}-py${{ matrix.python-version }}
          path: slixmpp-multiplatform/target/wheels/*.whl

  build-linux:
    name: Build for Linux
    needs: retrieve-package
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11', '3.12', '3.13', '3.14']

    runs-on: ubuntu-latest
    steps:
      - name: Download slixmpp
        uses: actions/download-artifact@v4
        with:
          name: slixmpp-multiplatform
          path: slixmpp-multiplatform

      - name: Build wheels using maturin container
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/io \
            -w /io/slixmpp-multiplatform \
            ghcr.io/pyo3/maturin \
            build --release --manylinux 2014 -i python${{ matrix.python-version }}

      - name: List wheels
        run: ls -lh slixmpp-multiplatform/target/wheels/

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: wheel-linux-py${{ matrix.python-version }}
          path: slixmpp-multiplatform/target/wheels/*.whl

  publish:
    name: Publish all wheels
    needs: [build-macos-silicon, build-macos-x86, build-win, build-linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist/

      - name: List wheels
        run: ls -R dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: dist/**/*.whl
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install maturin
        run: pip install maturin

      - name: Publish to PyPI
        run: |
          maturin upload dist/**/*.whl --username __token__ --password ${{ secrets.PYPI_TOKEN }}
